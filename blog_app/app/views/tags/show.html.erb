<h1>Articles linked with <%= @tag.name %> </h1>
<ul>
    <% @tag.articles.each do |article| %>
        <li><%= link_to article.title,  article_path(article) %> </li>
    <%end%>
</ul>
<%= link_to "view Tags", tags_path%><br/>
<%= link_to "back to articles <<", articles_path %>